default: &default
  adapter: postgresql
  encoding: unicode
  pool: <%= ENV.fetch("RAILS_MAX_THREADS") { 5 } %>

development:
  adapter: sqlite3
  database: myapp_development
test:
  adapter: sqlite3
  database: myapp_test
production:
  adapter: sqlite3
  database: myapp_production
